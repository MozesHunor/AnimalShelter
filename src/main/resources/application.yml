server.port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/animal-shelter-dog
    username: postgres
    password: postgres
  jpa:
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect